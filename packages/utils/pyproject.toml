[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openwatch-utils"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openwatch-config",
]

[tool.hatch.build.targets.wheel]
packages = ["openwatch_utils"]
