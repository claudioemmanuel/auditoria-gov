{
  "name": "@openwatch/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React component library for OpenWatch",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'No linter configured yet'",
    "typecheck": "tsc --noEmit 2>/dev/null || true",
    "build": "echo 'Build not configured yet'"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0"
  }
}
