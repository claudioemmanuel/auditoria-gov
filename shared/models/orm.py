# ORM model definitions for the public layer.
# These are registered against Base.metadata so alembic can detect schema drift.
# Table schemas are maintained via migration files in api/alembic/versions/.
#
# If you need to add a public-layer table, define a SQLAlchemy ORM class here
# using: from shared.models.base import Base
